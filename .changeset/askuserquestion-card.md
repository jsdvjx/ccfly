---
"ccfly": patch
---

The embedded web UI now renders `AskUserQuestion` with a dedicated, unfolded card — question header, all options (with the chosen one highlighted), and the selected answer shown inline — instead of a collapsed generic JSON dump that hid the answer.
